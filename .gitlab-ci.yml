image: maven:latest

build-job:
  stage: build
  script: chmod +x mvnw && ./mvnw package
  artifacts:
    paths:
      - target/build.jar
  only:
      - build

test-job:
  stage: test
  script:
    - echo "This job tests something"

