image: maven:latest

build-job:
  stage: build
  script: chmod +x mvnw && ./mvnw package
  artifacts:
    paths:
      - ./target/FlavourFit-0.0.1-SNAPSHOT.jar
  only:
      - build

test-job:
  stage: test
  script:
    - echo "This job tests something"

